# CryoNav — Antarctic Sea-Ice, Iceberg Trajectory & Navigation Decision Support
# Python 3.11–3.14 | Apple Silicon (MPS) / CUDA / CPU

# --- Core scientific stack ---
# numpy is intentionally unpinned on the upper bound: the project runs on numpy 2.x,
# and pinning <2.0 forces a source build (no 1.26 wheels for Python 3.13+).
numpy>=1.24
scipy>=1.11
xarray>=2023.6
zarr>=2.16
dask[array]>=2023.6      # required by xarray for chunked Zarr reads
netCDF4>=1.6             # xarray engine for the .nc files in the data-build path
h5netcdf>=1.2

# --- Geospatial ---
pyproj>=3.6
rasterio>=1.3            # used by src/data/sources/bathymetry.py

# --- ML ---
torch>=2.1

# --- Visualisation ---
matplotlib>=3.7

# --- API ---
fastapi>=0.104
uvicorn[standard]>=0.24
httpx>=0.25

# --- Utilities ---
pyyaml>=6.0
tqdm>=4.66
requests>=2.31
pandas>=2.0

# --- Tests ---
pytest>=7.4

# --- Optional data access (only needed to re-download the raw source datasets) ---
# cdsapi>=0.7.7            # ERA5 via Copernicus CDS
# copernicusmarine>=1.0    # CMEMS GLORYS
# earthaccess>=0.9         # NSIDC sea-ice concentration
# gdown>=5.0               # more reliable large-file Google Drive download

# --- Not currently imported anywhere; uncomment if you add map/plotting work ---
# cartopy>=0.22
# cmocean>=3.0
# shapely>=2.0
# rioxarray>=0.15
# scikit-learn>=1.3
# numba>=0.58
